#include "dominion.h"
#include "dominion_helpers.h"
#include "rngs.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int main()
{
	srand((NULL));
	struct gameState state;
	int treasure = 0;
		currPlayer=0,
		z=20,
		i=0,
		random=rand(),
		temp=array[z];
	
	for (i=0;i<100;i++)
		Adventurer(&state, currPlayer,rand,silver,temp,rand);

	return 0;
}
